@{
    ViewBag.Title = "Autenticar";
}
<link href="~/Content/Login.css" rel="stylesheet" type="text/css" />

<!-- Botón para abrir el modal de login -->
<button onclick="document.getElementById('id01').style.display='block'">Login</button>

<!-- Modal de Login -->
<div id="id01" class="modal">
    <span onclick="document.getElementById('id01').style.display='none'" class="close" title="Close Modal">&times;</span>

    <!-- Formulario de Login -->
    <form class="modal-content animate" action="/Acceso/Login" method="post">
        <div class="imgcontainer">
            <img src="@Url.Content("~/Content/avatar.jpg")" alt="Avatar" class="avatar" />
        </div>

        <div class="container">
            <label for="uname"><b>Usuario</b></label>
            <input type="text" placeholder="Ingrese usuario" name="uname" required>

            <label for="psw"><b>Contraseña</b></label>
            <input type="password" placeholder="Ingrese contraseña" name="psw" required>
        </div>

        <div class="login-section">
            <button type="submit">Login</button>
            <label>
                <input type="checkbox" checked="checked" name="remember"> Remember me
            </label>
        </div>

        <div class="container-bottom">
            <div class="bottom-row">
                <button type="button" onclick="document.getElementById('id01').style.display='none'" class="cancelbtn">Cancelar</button>
                <span class="psw">Olvidaste <a href="#">contraseña?</a></span>
            </div>

            <span class="register-link">
                ¿No tienes cuenta? <a href="@Url.Action("Registro", "Acceso")">Regístrate aquí</a>
            </span>
        </div>

    </form>
</div>

@section scripts {
    <script src="~/Scripts/Javascript.js"></script>
}
